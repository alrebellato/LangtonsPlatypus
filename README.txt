# Installation

Create a python virtual environment.

Install the requirements using pip and the requirements.txt file

# Usage

sandbox.py is a script that runs Langton's Platypus', enabling you to interactively create random platypus machines. Keybindings are output to the CLI upon the start of the script.

run with python sandbox.py
